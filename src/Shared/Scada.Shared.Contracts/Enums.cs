namespace Scada.Shared.Contracts;

public enum SensorQuality { GOOD, BAD, UNCERTAIN }

public enum SensorStatus { ACTIVE, INACTIVE, STANDBY }

public enum AlarmPriority { None = 0, Low = 1, Medium = 2, High = 3 }

apiVersion: v1
kind: Service
metadata:
  name: ingestion-service
  namespace: scada
spec:
  selector:
    app: ingestion-service
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: influxdb-ingestion
  namespace: scada
spec:
  selector:
    app: influxdb-ingestion
  ports:
    - port: 8086
      targetPort: 8086

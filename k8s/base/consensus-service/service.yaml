apiVersion: v1
kind: Service
metadata:
  name: consensus-service
  namespace: scada
spec:
  selector:
    app: consensus-service
  ports:
    - port: 8080
      targetPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-consensus
  namespace: scada
spec:
  selector:
    app: postgres-consensus
  ports:
    - port: 5432
      targetPort: 5432
